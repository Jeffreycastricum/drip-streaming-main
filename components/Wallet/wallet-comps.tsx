import React from 'react'

const Wallet = () => {
  return (
    <div className="wallet-container">
      <div className="wallet-button">
        {/* Here we will have a link for a conneting to wallet */}
        <button type="button">Connect Wallet</button>
      </div>
    </div>
  )
}

export default Wallet
